version: '3.8'

services:
  # Main test-yaml-final-2 service
  test-yaml-final-2:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: test-yaml-final-2:latest
    container_name: test-yaml-final-2
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=production
      - DEBUG=false
      - LOG_LEVEL=INFO
      - DATABASE_URL=postgresql+asyncpg://test-yaml-final-2:password@postgres:5432/test-yaml-final-2_db
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY:-your-secret-key-here}
      - ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
      - ALLOWED_HOSTS=localhost,127.0.0.1
    volumes:
      # Mount logs directory for persistence
      - test-yaml-final-2_logs:/app/logs
      # Mount uploads directory for file storage
      - test-yaml-final-2_uploads:/app/uploads
      # Mount cache directory for caching
      - test-yaml-final-2_cache:/app/cache
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
      - /var/tmp

  # Development service with hot reload
  test-yaml-final-2-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    image: test-yaml-final-2:dev
    container_name: test-yaml-final-2-dev
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=development
      - DEBUG=true
      - LOG_LEVEL=DEBUG
      - DATABASE_URL=postgresql+asyncpg://test-yaml-final-2:password@postgres:5432/test-yaml-final-2_db
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=dev-secret-key
      - ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
      - ALLOWED_HOSTS=localhost,127.0.0.1
    volumes:
      # Mount source code for hot reload
      - .:/app
      # Mount logs directory
      - test-yaml-final-2_logs:/app/logs
      # Mount uploads directory
      - test-yaml-final-2_uploads:/app/uploads
      # Mount cache directory
      - test-yaml-final-2_cache:/app/cache
    ports:
      - "8001:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    profiles:
      - dev

  # PostgreSQL database
  postgres:
    image: postgres:15-alpine
    container_name: test-yaml-final-2-postgres
    environment:
      - POSTGRES_DB=test-yaml-final-2_db
      - POSTGRES_USER=test-yaml-final-2
      - POSTGRES_PASSWORD=password
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8 --lc-collate=C --lc-ctype=C
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    ports:
      - "5432:5432"
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test-yaml-final-2 -d test-yaml-final-2_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true

  # Redis cache
  redis:
    image: redis:7-alpine
    container_name: test-yaml-final-2-redis
    command: redis-server --appendonly yes --requirepass password
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    security_opt:
      - no-new-privileges:true

  # Celery worker for background tasks
  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: test-yaml-final-2:latest
    container_name: test-yaml-final-2-celery-worker
    command: celery -A app.core.celery worker --loglevel=info
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql+asyncpg://test-yaml-final-2:password@postgres:5432/test-yaml-final-2_db
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY:-your-secret-key-here}
    volumes:
      - test-yaml-final-2_logs:/app/logs
      - test-yaml-final-2_uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    profiles:
      - worker

  # Celery beat for scheduled tasks
  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: test-yaml-final-2:latest
    container_name: test-yaml-final-2-celery-beat
    command: celery -A app.core.celery beat --loglevel=info
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql+asyncpg://test-yaml-final-2:password@postgres:5432/test-yaml-final-2_db
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY:-your-secret-key-here}
    volumes:
      - test-yaml-final-2_logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    profiles:
      - worker

  # Flower for monitoring Celery tasks
  flower:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: test-yaml-final-2:latest
    container_name: test-yaml-final-2-flower
    command: celery -A app.core.celery flower --port=5555
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=production
      - REDIS_URL=redis://redis:6379/0
    ports:
      - "5555:5555"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    profiles:
      - worker

  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    container_name: test-yaml-final-2-nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - test-yaml-final-2_uploads:/var/www/uploads:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - test-yaml-final-2
    networks:
      - test-yaml-final-2_network
    restart: unless-stopped
    profiles:
      - nginx

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  test-yaml-final-2_logs:
    driver: local
  test-yaml-final-2_uploads:
    driver: local
  test-yaml-final-2_cache:
    driver: local

networks:
  test-yaml-final-2_network:
    driver: bridge
